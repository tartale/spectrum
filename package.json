{
  "name": "spectrum",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@11.15.0",
  "scripts": {
    "dev": "pnpm --filter mobile start",
    "web": "pnpm --filter mobile web",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "db:start": "supabase start",
    "db:reset": "supabase db reset",
    "db:seed": "supabase db reset && node supabase/seed/seed.mjs"
  },
  "devDependencies": {
    "supabase": "^2.30.4",
    "typescript": "~5.8.3"
  }
}
